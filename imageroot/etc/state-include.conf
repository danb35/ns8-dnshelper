#
# dnshelper state/backup include patterns for Restic
# Syntax reference: https://pkg.go.dev/path/filepath#Glob
#
# Zones, the provider credentials they use, and which modules may change what. Credentials are included on
# purpose: without them a restored module cannot manage any zone. They are
# protected by the backup destination's encryption (Restic), like the rest of
# the module state. See DESIGN.md, Credentials.
state/zones.json
state/policy.json
state/credentials
